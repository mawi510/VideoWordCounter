version: '3.8'

services:
  app:
    build: .
    ports:
      - "8501:7860"
    volumes:
      - .:/home/user/app
    environment:
      - PYTHONUNBUFFERED=1
    container_name: video-word-counter
    restart: unless-stopped